==Установка Arch на Gnome (если не получается по базе)==
1. На моменте установки в archinstall устанавливаем profile вместо desktop, где ставили гном, ставим просто minimal,  дальше как обычно качаем
2. Как скачается, выключаем по базе, вытаскиваем диск установочный и включаем, откроется терминал, вводим логин пароль и прописываем команды:
sudo pacman -Syu gnome (это мы просто интерфейс гнома качаем)
sudo systemctl enable gdm (это мы включаем как я понял экран блокировки (greeter)) 
reboot

Возможно понадобится уже в самой ос скачать nano, но это не точно
sudo pacman -S nano

===Просмотр настроек ядра===
1. Проверка версии ос:
uname -a
2. Проверка параметров, которые передаются ядру при загрузке системы (Boot параметры):
cat /proc/cmdline
3. Просмотр параметров, связанных с управлением памяти(swap - выгрузка данных из оперативки на жесткий диск) и реакцией системы на критические ошибки;
параметр vm.swappiness от 0(избегание использования swap раздела) до 100(активное использование swap, даже когда в оперативной памяти нет места), настройка будет временной (до перезагрузки);
kernel.panic от 0(Система не перезагружается после критической ошибки) до n секунд, после которых система перезагружается; 
все остальные параметры kernel.panic показывают либо 0 (продолжить работу после ошибки), либо 1 – принудительно вызвать kernel panic (перезагрузка для безопасности), например: kernel.panic_on_io_nmi - ошибки ввода-вывода:
sysctl -a | grep -e "vm.swappiness" -e "kernel.panic"
4. Просмотр установленных модулей ядра:
lsmod

Дополнительно можно продемонстрировать настройку swappiness(настройка, указывающая системе, когда начинать 'выгружать' неиспользуемые данные из оперативной памяти на жесткий диск);
от 0(избегание использования swap раздела) до 100(активное использование swap, даже когда в оперативной памяти нет места), настройка будет временной (до перезагрузки):
sudo sysctl vm.swappiness=30


===SSH===
Нужен сетевой мост!!
1. Настройки -> system -> Secure Shell -> нажимаем ✓
2. Узнаем ip: 
ip addr
3. С основного ПК прописываем:
ssh login@ip (Пример: user@10.201.83.146) 
4. Вводим для проверки ls 


===Удаленный доступ===
Нужен сетевой мост!!
1. Настройки -> system -> Remote Desktop -> включаем две верхние ✓
2. в Login details прописываем если нужно новый логин и пароль
3. В терминале прописываем ip addr и смотрим ip 
4. Выключаем ВМ и правой кнопкой мыши по ВМ клонируем её, далее нажимаем клонировать (Сгенерировать новые mac адреса)
5. Запускаемся со второй ВМ и запускаем программу Connections
6. Нажимаем + и вводим ip первой ВМ, на которой включили удалённый доступ, если не получается, вводим еще порт (192.168.1.103:3389)
7. Нажимаем Verify, затем вводим данные, которые добавляли ранее


===Проверка сетевого соединения===
Нужен сетевой мост!!
ip addr
или
nmcli device


===Виртуальный принтер===
1. Установка компонентов:
sudo pacman -Syu cups cups-pdf ghostscript
2. Запуск служб печати:
sudo systemctl enable cups.service
sudo systemctl start cups.service
3. Создание папки для pdf-файлов:
mkdir -p ~/название_папки
4. Открываем файл конфигурации CUPS:
sudo nano /etc/cups/cups-pdf.conf
5. Находим строку, начинающуюся с Out и меняем ее на:
Out /home/имя_пользователя/название_папки_из_3_пункта
Сохраняем и закрываем:
Ctrl + x -> y -> ENTER
6. Перезапуск CUPS:
sudo systemctl restart cups.service

Пишем в браузере ссылку:
http://localhost:631
Выбираем «Администрирование», затем «Принтеры» и «Добавить принтер», вводим логин и пароль
В списке доступных вариантов выбрать «CUPS-PDF (Virtual PDF Printer)»
Настроить параметры принтера: задать его имя, выбрать «разрешить совместный доступ к этому принтеру»
Выбрать «Generic»
Выбрать драйвер для принтера — «Generic CUPS-PDF Printer (w/options) (en)». Нажать «Добавить принтер»
Во вкладе принтеры. Настроить параметры печати по умолчанию.
Чтобы проверить работает ли принтер:

Выбираем «Печать пробной страницы»
Созданные файлы появятся по пути /home/имя_пользователя/название_папки_из_3_пункта